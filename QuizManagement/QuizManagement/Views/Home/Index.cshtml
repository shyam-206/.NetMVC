@model List<QuizManagement_Model.ViewModel.QuizModel>

<div class="container mt-5">
    

    <div class="card mt-5">
        <div class="card-body">
            <h4 class="text-center" style="color: #2A629A;">Quiz List</h4>
            <div>
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <td>Title</td>
                            <td>Description</td>
                            <td>Action</td>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <a href="CreateQuestion">@item.title</a>
                                    </td>
                                    <td>@item.description</td>
                                    <td>
                                        <div>
                                            @Html.ActionLink("Start","QuizStart", new { quiz_id = item.quiz_id, Controller = "Home" }, new { @class = "btn btn-success sm" })
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>